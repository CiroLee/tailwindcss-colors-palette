{
  "name": "tailwindcss-colors-palette",
  "version": "0.0.1-beta",
  "type": "module",
  "main": "dist/index.css",
  "style": "dist/index.css",
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./dist/index.css",
    "./colors/ant.css": "./dist/colors/ant.css",
    "./colors/universal.css": "./dist/colors/universal.css"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build:colors": "pnpm run clean && node build-colors.js",
    "build": "pnpm run build:colors",
    "dev": "node dev-colors.js --watch",
    "dev:build": "node dev-colors.js"
  },
  "peerDependencies": {
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "autoprefixer": "^10",
    "chokidar": "^3.6.0",
    "postcss": "^8",
    "postcss-cli": "^11",
    "postcss-import": "^16",
    "prettier": "^3.6.2"
  }
}
